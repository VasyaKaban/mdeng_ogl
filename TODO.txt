SPEC: https://registry.khronos.org/OpenGL/specs/gl/glspec46.core.pdf

Change DDS image info that will return self-created image info not a Render::ImageInfo
hrs::forward_pool -> for what???
map<ClassID, list<Handler>> and map<ClassID, map<EvntObject*, Handler>> for events -> reudce memory usage
Add limits and features
Add queries
Add transform feedback
Move to OpenGL 4.5 + use extensions to populate features
FreeBlockList: Split begin and after iterators in free block! In for loop -> after always be the next of before
"SHOULD WE SET BARRIER FOR COPY AFTER FRAGMENT SHADER???" -> in Engine???
glTextureStorage3D -> should we mul by 6 array layers???
OpenGL/ES and D3D compatibily for Vulkan as features
explicit dllimport/dllexport + INTERFACE targets
cache Sampler, DescriptorSetLayout and PipelineLayout(for VK)
VK_EXT_primitive_topology_list_restart for lists in VK as feature
VK_EXT_border_color_swizzle???
FormatToNative conversions for retrieving format properties shouldn't throw exceptions(OpenGL!)
add immutable samplers
OS-dependent context creation for OpenGL
Pipeline state based on enabled features for OpenGL
Check all PipelineInfo* structs and compare them with Vulkan spec
GL_KHR_no_error -> should we add?
constants like VK_LOD_CLAMP_NONE and other?
Check throw in commands and creation functions -> OGL only
Change descriptor set binding -> instead of set and linear model use union of models + API type
delete default statements in switch for formats -> populate cases via clangd quick fix
check all static vectors and merge them into one common vector for temp info
after adding new formats also update DDS so it can handle formats with shifts
https://registry.khronos.org/OpenGL/specs/gl/glspec46.core.pdf page: 222
Descriptor descriptions:
     struct DescriptorDescription
    {
        std::uint32_t set;
        std::uint32_t opengl_binding;
        std::uint32_t vulkan_binding;
        std::uint32_t directx_binding;
    };

    //select binding(DescriptorDescription) -> if context.backend == OpenGL return opengl_binding...
int16, int64, float16? for uniforms
Create default framebuffers based on renderpass
Check format type - we cannot get info about it before context creation