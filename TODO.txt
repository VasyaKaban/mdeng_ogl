INFO:
    SPEC: https://registry.khronos.org/OpenGL/specs/gl/glspec46.core.pdf

* hrs::forward_pool: for what?
* Add queries fo render: investigate
* Add transform feedback: investigate
* FreeBlockList: Split begin and after iterators in free block! In for loop -> after always be the next of before: IDK?
* SHOULD WE SET BARRIER FOR COPY AFTER FRAGMENT SHADER???: in Engine???
* glTextureStorage3D: should we mul by 6 array layers???
* OpenGL/ES and D3D compatibily for Vulkan as features: just check VK spec
* Vulkan formats from VK_EXT_4444_formats and VK_KHR_maintenance5: we should use feature for them to explicitly enable either extension or core version
* VK_EXT_primitive_topology_list_restart for lists in VK as feature
* VK_EXT_border_color_swizzle: ???
* Check all PipelineInfo* structs and compare them with Vulkan spec
* Check throw in commands and creation functions: OGL only
* Change descriptor set binding: instead of set and linear model use union of models + API type(just for high-level descriptor infos in Engine...). Maybe as virtual function to return descriptor infos
* nonCoherentAtomSize: use in VK as inner parameter for mappings(maybe allocate buffers with alignment corrected to this size + size also align)
* Change plain pointer to unique_ptr due to the exceptions in constructor(see Device ctor!)
* Add enabled features check for every object like aniso in sampler and in pipeline state
* Only one active Instance and legacy device for OpenGL backend
* move hrs to Core
* Common naming for FlagBits
* pipeline binary retrieve: format as pipelineCacheUUID(fill with zeroes in VK) + API flag -> use VkPipelineCache for VK?
* OGL-only formats like R3G3B2
* More dynamic states: TBA???
* debug_utils: add object labels? Is VAO represent the same concept like VkPipelineLayout in Vulkan?
* Memory:
    In vulkan:
        if we map buffers each map call wil increment map counter in inner memory handle
        each unmap also will decrement inner map counter
    Maybe add methods for device that will get info about:
        two passed in buffers are bound to the same inner memory handle
        FlushMappedRanges that will receive array of memory ranges to flush and buffers and create common arrays for VK? + same for invalidate -> do we really need to map a lot of buffers to use them within the same frame?
    buffer align: common(bufferImageGranularity, nonCoherentAtomSize, req.alignment)
    image align: common(bufferImageGranularity,req.alignment)
* dllexport/dllimport for linux(mostly for templates): use extern template?
* max_image_array_layers for surface capabilities and swapchain
* change enum to enum class + use template operator|
* remove '_' prefix from methods and constructors and use explicit this! 
* WindowSubsystem and Window classes:
    Window as Abs class
    WindowSubsystem as Abs class
    Backends: Win32, X11(maybe XCB + libxkbcommon), Wayland(with libxkbcommon)
* Get methods as copy values instead of references
* dlerror() into System for Linux
* Limits and properties for window subsystem: for example limit max window size

* Check Win32 windowed_state updates
* More preciese window create info
* Window Hidden/Shown as state + functions
* Do not create temporary objects in constructors. Just write directly into fields
* One-value enums as enum class; Flags as Flags using + non-scoped enum inside namespace with FlagBits postfix
* When we change video mode of one monitor we receive diplsyachange event. So we must change position and prev window rect for window also in fullscreen mode
* [Render] add methods to get CreateInfo struct from each objects
* Window fullscreen:
    - from windowed to fullscreen -> save prev and go to fullscreen
    - from fullscreen to windowed -> read prev and go to windowed
    - every move and resize save prev 
    - if fullscreen is moved or resized -> deny
    - if fullscreen changed displays -> accept move or resize and set fullscreen of new display
* (exe)Game -> (dll)Engine.Init -> Engine.ProbeRenders(renders...) -> Engine.CreateObjects(Window, ...) -> Engine.SetFrameHandler(...) -> Engine.Run()
* Clipboard
* Split methods into get count and get data for compound-allocated types
* View -> WindowSubsystem, WindowSubsystem -> WindowSubsystemConnection
* Remove namespaces for Linux/Win32 and mark classes with appropriate prefixes
* OpenGL backend as base classes?
* Non-new allocated clipboard object
* More clipboard formats: Dran'n'Drop file list
* FunctionRef
* RCPointer -> explicit instantitation for array types due to the operator delete[] need!

Instance:
    GetProperties ->
        InstanceProperties:
            bool exposition_only_physical_devices
    GetFeatures ->
        InstanceFeatures:
            bool validation_layer
            bool debug_messenger
    EnumeratePhysicalDevices() ->
        PhysicalDevice:
            bool exposition_only(true for OGL and false for VK)
            char name
            PhysicalDeviceType type
            QueueFamilyDesc queue_family_descriptions
            ...

    Surface:

    CreateDevice(PhysicalDevice, Surface, PhysicalDeviceFeatures[enabled], EnabledImageFormats[]) -> 
        Device:
            GetProperties
            GetFeatures
            GetSwapchain
            ...

Do not care about move_if_noexcept -> require move to be noexcept in all old-to-new copy
Vector<T> + remove Resize and all default_value and add Unchecked push_back(without checks)
Stack<T>
Queue<T>
Heap<T, Comparator>
Map(via MultiKey)<V, K, Keys...> -> Key(s) as Key typename, Comparator typename and policy 
UnorderedMap//Map and UnorderedMap with policy<V, K, Keys...> -> Key(s) as Key typename, EqualComparator typename, hasher typename and policy 
StaticVector(like array but with reserve and push_back)<T, SIZE>
Path
PathView???
File
String<char8_t, char16_t, char32_t> + AsNative():
	char8_t -> char
	char16_t -> wchar_t on Win32 and char16_t elsewhere
	char32_t -> wchar_t on non-Win32 targets and char32_t on Win32
StringCharEncoder:
	size_t GetInputDataSize(CHAR[any char that can be converted]* input, size_t size)
	isize_t/*codepoint size: 0 -> end, > 0 -> success, < 0 -> error*/ GetNextCodePoint(CHAR[any char that can be converted]* input, size_t size, size_t& offset);
String<CHAR, StringCharEncoder>	
StringView<CHAR>
NullTerminatedStringView<CHAR> -> throw exception on every innser string change when last char is not null-terminated
Ring<T, SIZE>
Pool<T>
RawPool
Union<Types...>
Any
Optional<T>
Expected<T, E>
Callable<R (Args... ?...) ?>
Tuple<Types...>
Thread + Semaphores, barriers, fences, mutexes, ...
Bitset???
Matrix, vector???
regex -> via PCRE???

RCPointer:
	T* ptr
	where T: 
		size_t IncrementRefs() noexcept;
		size_t DecrementRefs() noecxept;
		void operator delete(T* ptr) noexcept;
		
		
class RC -> non-atomic counter. delete via class
class ARC -> atomi counter. delete via class

	
class Container:
	...
	Allocator* allocator;
	
	+ void* ReleaseMemory(bool destroy_objects)
	
class RCPointer:
	T* ptr
	Allocator* allocator
	
	+ void* ReleaseMemory(bool destroy_objects);
	
class UniquePointer:
	T* ptr
	Allocator* allocator

	+ void* ReleaseMemory(bool destroy_objects);//use it when we wnat to handle object itself or when we reset the inner allocator explicitly
	

class Allocator:
	virtual void* Allocate(const MemoryRequirements& req) = 0;
	virtual void Deallocate(void* ptr) = 0;
	
	+ void ResetAllocations();

aligned(min=sizeof(u64)) PoolNode:
    ...padding in case of small block before
    Header:
        u32 before_padding_size
        u1 is_free
        u64 size
        u64 next
        u64 prev
        u64 next_free
        u64 prev_free
    ...data(aligned to data alignment)
    ...pad to fill PollNode alignment(sizeof(u64))

