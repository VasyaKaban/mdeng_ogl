INFO:
    SPEC: https://registry.khronos.org/OpenGL/specs/gl/glspec46.core.pdf

* hrs::forward_pool: for what?
* Add queries fo render: investigate
* Add transform feedback: investigate
* FreeBlockList: Split begin and after iterators in free block! In for loop -> after always be the next of before: IDK?
* SHOULD WE SET BARRIER FOR COPY AFTER FRAGMENT SHADER???: in Engine???
* glTextureStorage3D: should we mul by 6 array layers???
* OpenGL/ES and D3D compatibily for Vulkan as features: just check VK spec
* Vulkan formats from VK_EXT_4444_formats and VK_KHR_maintenance5: we should use feature for them to explicitly enable either extension or core version
* VK_EXT_primitive_topology_list_restart for lists in VK as feature
* VK_EXT_border_color_swizzle: ???
* Check all PipelineInfo* structs and compare them with Vulkan spec
* Check throw in commands and creation functions: OGL only
* Change descriptor set binding: instead of set and linear model use union of models + API type(just for high-level descriptor infos in Engine...). Maybe as virtual function to return descriptor infos
* nonCoherentAtomSize: use in VK as inner parameter for mappings(maybe allocate buffers with alignment corrected to this size + size also align)
* Change plain pointer to unique_ptr due to the exceptions in constructor(see Device ctor!)
* Add enabled features check for every object like aniso in sampler and in pipeline state
* Only one active Instance and legacy device for OpenGL backend
* move hrs to Core
* Common naming for FlagBits
* pipeline binary retrieve: format as pipelineCacheUUID(fill with zeroes in VK) + API flag -> use VkPipelineCache for VK?
* OGL-only formats like R3G3B2
* More dynamic states: TBA???
* debug_utils: add object labels? Is VAO represent the same concept like VkPipelineLayout in Vulkan?
* Memory:
    In vulkan:
        if we map buffers each map call wil increment map counter in inner memory handle
        each unmap also will decrement inner map counter
    Maybe add methods for device that will get info about:
        two passed in buffers are bound to the same inner memory handle
        FlushMappedRanges that will receive array of memory ranges to flush and buffers and create common arrays for VK? + same for invalidate -> do we really need to map a lot of buffers to use them within the same frame?
    buffer align: common(bufferImageGranularity, nonCoherentAtomSize, req.alignment)
    image align: common(bufferImageGranularity,req.alignment)
* dllexport/dllimport for linux(mostly for templates): use extern template?
* max_image_array_layers for surface capabilities and swapchain
* change enum to enum class + use template operator|
* remove '_' prefix from methods and constructors and use explicit this! 
* WindowSubsystem and Window classes:
    Window as Abs class
    WindowSubsystem as Abs class
    Backends: Win32, X11(maybe XCB + libxkbcommon), Wayland(with libxkbcommon)
* Get methods as copy values instead of references
* dlerror() into System for Linux
* Limits and properties for window subsystem: for example limit max window size
* WindowPosition as uint and MousePosition as int relative to window we want to capture
* Errors in window procedures: throw exceptions or just ignore?
* Add Display(Monitor) class that will be bound to window and will be updated on event
* final spec for WindowSubsystem classes