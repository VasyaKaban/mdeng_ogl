SPEC: https://registry.khronos.org/OpenGL/specs/gl/glspec46.core.pdf

hrs::forward_pool -> for what???
map<ClassID, list<Handler>> and map<ClassID, map<EvntObject*, Handler>> for events -> reudce memory usage
Add limits and features
Add queries
Add transform feedback
FreeBlockList: Split begin and after iterators in free block! In for loop -> after always be the next of before
"SHOULD WE SET BARRIER FOR COPY AFTER FRAGMENT SHADER???" -> in Engine???
glTextureStorage3D -> should we mul by 6 array layers???
OpenGL/ES and D3D compatibily for Vulkan as features
explicit dllimport/dllexport + INTERFACE targets
VK_EXT_primitive_topology_list_restart for lists in VK as feature
VK_EXT_border_color_swizzle???
Pipeline state based on enabled features for OpenGL
Check all PipelineInfo* structs and compare them with Vulkan spec
Check throw in commands and creation functions -> OGL only
Change descriptor set binding -> instead of set and linear model use union of models + API type
delete default statements in switch for formats -> populate cases via clangd quick fix
check all static vectors and merge them into one common vector for temp info
https://registry.khronos.org/OpenGL/specs/gl/glspec46.core.pdf page: 222
Descriptor descriptions:
     struct DescriptorDescription
    {
        std::uint32_t set;
        std::uint32_t opengl_binding;
        std::uint32_t vulkan_binding;
        std::uint32_t directx_binding;
    };

    //select binding(DescriptorDescription) -> if context.backend == OpenGL return opengl_binding...
#nonCoherentAtomSize -> use in VK as inner parameter for mappings(maybe allocate buffers with alignment corrected to this size + size also align)
remove #include winapi from Render -> change to forward declaration
remove multi-jump in pipeline state(maybe add caching with pointers to functions?)

Change plain pointer to unique_ptr due to the exceptions in constructor(see Device ctor!)
Add enabled features check for every object like aniso in sampler and in pipeline state
Make physical device current on device delete notify + think abound make current and reset previous current context

move hrs to Core
Common naming for FlagBits
pipeline binary retrieve -> format as pipelineCacheUUID(fill with zeroes in VK) + API flag -> use VkPipelineCache for VK?

More dynamic states? -> to be added...
debug_utils -> add object labels? Is VAO represent the same concept like VkPipelineLayout in Vulkan?
 
Should we use UnknownQueueSpec? AFAIK other flags like video encode/decode only appear on video encode/decode extension provided? -> but it will be listed anyway because we enable extensions on device creation while queue properties we get from physical device
In vulkan:
    if we map buffers each map call wil increment map counter in inner memory handle
    each unmap also will decrement inner map counter
Maybe add methods for device that will get info about:
    two passed in buffers are bound to the same inner memory handle
    FlushMappedRanges that will receive array of memory ranges to flush and buffers and create common arrays for VK? + same for invalidate -> do we really need to map a lot of buffers to use them within the same frame?

buffer align: common(bufferImageGranularity, nonCoherentAtomSize, req.alignment)
image align: common(bufferImageGranularity,req.alignment)
Maybe add memory_allocation_size_hint for vulkan memory allocator as field in DeviceInfo? -> NoOp in OGL
